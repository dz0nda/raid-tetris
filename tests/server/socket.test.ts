// import { destroySocket, initSocket } from './helpers/socket';

// describe('[e2e] Socket', () => {
//   describe('[Socket] Connection', () => {
//     it('[Socket] Connect should succeed', async () => {
//       const socket = await initSocket(3000);
//       expect(socket.connected).toBe(true);

//       await destroySocket(socket);
//       expect(socket.connected).toBe(false);
//     });
//   });
// });
